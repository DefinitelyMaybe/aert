{
  "dependencies": {
    "three": "^0.118.3",
    "typescript": "^3.9.7"
  },
  "scripts": {
    "fmt": "deno fmt src && deno fmt utils",
    "lint": "deno lint --unstable src",
    "compile-ts": "npx tsc",
    "compile-svelte": "",
    "watch-rename-urls": "deno run -A ./utils/watch-and-rename.ts",
    "build-copy-threejs": "deno run -A ./utils/build-copy-three.ts",
    "build-bundle-from-main": ""
  }
}
