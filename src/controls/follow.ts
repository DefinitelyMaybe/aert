class FollowController {
  constructor() {
    // aiming for:
    // const controls = new FollowControls(camera, renderer.domElement)
    // controls.follow( object3d )

    // perhaps simply extend the orbit controls
    // but adjust it appropriately
    // i.e. cannot move object, can rotate object to face camera direction

  }
}

export { FollowController }