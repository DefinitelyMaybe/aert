class ObjectController {
  constructor() {
    // Attach to an Object and camera
    // i.e. new ObjectController(mesh, camera)

    // Attach dom listeners for key presses
    // move camera and object appropriately

    // Bonus features
    // enabled, speed
  }
}

export { ObjectController }