class ObjectController {
  constructor() {
    // Attach to an Object
    // i.e. new ObjectController(Mesh)

    // Attach dom listeners for key presses

    // Bonus features
    // enabled, speed
  }
}

export { ObjectController }